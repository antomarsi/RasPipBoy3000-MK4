# Visual theme (colors) is no longer set here -- it lives in save/state.json
# (game/data/store.py's ThemeSettings), since it's user-changeable state, not
# restart-only deployment config. Edit save/state.json (or save/state.example.json
# for the default shape) to change DRAW_COLOR/TINT_COLOR/BG_COLOR-equivalents.

# SKIP_INTRO=false
# USE_SCANLINE=true
# USE_BLUR=true
# RESCALE=false
# FRAMERATE=60
# IDLE_FRAMERATE=15
# STARTUP_MODULE=map
